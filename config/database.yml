default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  username: postgres
  password:
  pool: 5
  host: <%= ENV['POSTGRES_HOST'] %>

development:
  <<: *default
  database: users-db


test:
  <<: *default
  database: users-db
